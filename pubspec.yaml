name: todolistapp
description: "A simple Flutter To-Do List app with local data storage (Hive)."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2




  # 📦 Tambahan dependencies untuk penyimpanan data lokal
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.6   # 🛠️ diperlukan untuk generate file Hive adapter

flutter:
  uses-material-design: true
